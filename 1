add Kafkr::Consumer::HANDLERS_DIRECTORY
# Please enter the commit message for your changes. Lines starting
# with '#' will be ignored, and an empty message aborts the commit.
#
# On branch main
# Your branch is ahead of 'origin/main' by 3 commits.
#   (use "git push" to publish your local commits)
#
# Changes to be committed:
#	modified:   exe/kafkr-consumer
#
# ------------------------ >8 ------------------------
# Do not modify or remove the line above.
# Everything below it will be ignored.
diff --git a/exe/kafkr-consumer b/exe/kafkr-consumer
index 1e08a29..27c3a91 100755
--- a/exe/kafkr-consumer
+++ b/exe/kafkr-consumer
@@ -51,6 +51,7 @@ def monitor_handlers
 
   loop do
     changed = false
+    puts "checking #{Kafkr::Consumer::HANDLERS_DIRECTORY} for changes!"
     Find.find(Kafkr::Consumer::HANDLERS_DIRECTORY) do |path|
       if File.file?(path) && File.mtime(path) > last_checked
         load path
